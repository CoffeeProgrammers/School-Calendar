import React, {useState, useEffect} from 'react';
import {Typography} from "@mui/material";

const Main = () => {
    return (
        <Typography variant="h4">
            Main
        </Typography>
    );
};

export default Main;